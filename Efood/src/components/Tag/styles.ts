import styled from 'styled-components'
import { color } from '../../styles'

const TagRestaurant = styled.div`
  padding: 6px 10px;
  background-color: ${color.darkOrange};
  color: ${color.orange};
  display: inline-block;
  font-size: 12px;
  font-weight: bold;
`
export default TagRestaurant
